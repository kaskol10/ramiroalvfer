name = "ramiroalvfer"
compatibility_date = "2025-09-01"
pages_build_output_dir = "dist"

# Uncomment after: npx wrangler d1 create k8scockpit-broadcast
# Then paste database_id and run: npm run db:migrate:remote
# Also bind DB → k8scockpit-broadcast in Pages → Settings → Functions.
#
# [[d1_databases]]
# binding = "DB"
# database_name = "k8scockpit-broadcast"
# database_id = "YOUR_D1_DATABASE_UUID"
# migrations_dir = "migrations"

[vars]
PUBLIC_SITE_URL = "https://k8scockpit.tech"
# NEWSLETTER_FROM = "K8sCockpit <broadcast@k8scockpit.tech>"

# Secrets (Pages dashboard or `wrangler pages secret put`):
#   TURNSTILE_SECRET_KEY
#   RESEND_API_KEY          (optional — enables double opt-in email)
